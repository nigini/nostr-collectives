# ── Relay (caped-bucket) ──
PORT=3334
RELAY_NAME=NosColl Relay
RELAY_DESCRIPTION=A Nostr Collectives relay
RELAY_PUBKEY=
RELAY_ICON=
EVENT_TTL=86400000

# ── Docker ports (host-side) ──
RELAY_PORT=3334
CLIENT_PORT=8080

# ── Client (comma-separated relay URLs) ──
DEFAULT_RELAYS=ws://localhost:3334
